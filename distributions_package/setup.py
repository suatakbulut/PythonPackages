from setuptools import setup

setup(name = 'distributions', 
	version= '0.1', 
	description= 'A simple python package example with Gaussian Distribution',
	packages=['distributions'], 
	zip_safe = False
	)